-@posts.each do |post|
  %article
    %h2
      %a{ :href => post.url }
        = post.title
    %span.posted
      Posted
      = post.posted.strftime "%Y-%m-%d"
      @
      = post.posted.strftime "%H:%M"
      UTC
    = post.html